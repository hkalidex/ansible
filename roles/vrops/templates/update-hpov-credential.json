{
    "id": "{{ item.id }}",
    "name": "{{ item.name }}",
    "adapterKindKey": "{{ item.adapterKindKey }}",
    "credentialKindKey": "{{ item.credentialKindKey }}",
    "fields": [{
        "name": "UserName",
        "value": "{{ item.username }}"
    }, {
        "name": "Password",
        "value": "{{ item.password }}"
    }, {
        "name": "Directory",
        "value": "{{ item.directory }}"
    }, {
        "name": "RestUsername",
        "value": "{{ item.vrops_username}}"
    }, {
        "name": "RestPassword",
        "value": "{{ item.vrops_password }}"
    }],
    "others": [],
    "otherAttributes": {}
}