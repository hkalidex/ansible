{
    "id": null,
    "name": "{{ item.name }}",
    "adapterKindKey": "HPOneviewVCopsAdapter3",
    "credentialKindKey": "hpOneviewCredentialsv2",
    "fields": [{
        "name": "UserName",
        "value": "{{ item.username }}"
    }, {
        "name": "Password",
        "value": "{{ item.password }}"
    }, {
        "name": "Directory",
        "value": "{{ item.directory }}"
    }, {
        "name": "RestUsername",
        "value": "{{ item.vrops_username }}"
    }, {
        "name": "RestPassword",
        "value": "{{ item.vrops_password }}"
    }]
}